skills/
node_modules/

# Build output — generated, must not be committed
.next/
tsconfig.tsbuildinfo

# Ruflo local secrets and runtime data
.env
.env.local
.env.*.local
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/
scratchpad-backups/

# Runtime logs (generated by `npm run dev` via tee)
dev.log

# Local SQLite database (only used when DB_BACKEND=prisma — legacy)
db/custom.db
package-lock.json
